$(document).ready(function () {
    $('#left-nav').remove();
    $('#banner').remove();

    $('.selectpicker').selectpicker({
        liveSearch: true,
        showSubtext: true,
        Size: 10
    });
});